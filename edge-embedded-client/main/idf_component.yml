dependencies:
  idf: ">=5.4"
  livekit/livekit:
    version: "0.3.4"
  livekit/sandbox_token: ~0.1.0
  livekit/example_utils: ~0.2.0
  tempotian/codec_board: ~1.0.0
  espressif/cjson: "^1.0.0"
  # WiFi remote components for ESP32-P4 (uses ESP32-C6 co-processor)
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  espressif/esp_hosted:
    rules:
      - if: "target in [esp32p4, esp32h2]"
